# Core formatting logic and internal helpers for {{PACKAGE_NAME}}
# Part of the Alya Language package ecosystem (https://github.com/alya-lang)

import "../types.alya"

# Internal helper using when pattern matching (private to module, not exported with pub)
function build_salutation(prefix, style)
    return when style
        is TemplateStyle.Formal => "Greetings and " + prefix
        is TemplateStyle.Casual => "Hey"
        else                    => prefix
    end
end

# Public formatting function (exported via pub)
pub function core_format_greeting(name)
    return "Hello, " + name + "!"
end

# Public custom formatter using struct and pattern matching (exported via pub)
pub function core_format_custom(config)
    let salutation = build_salutation(config.prefix, config.style)
    return salutation + ", " + config.name + "!"
end
