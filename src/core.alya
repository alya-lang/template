# Core logic and internal helpers for {{PACKAGE_NAME}}
# Part of the Alya Language package ecosystem (https://github.com/alya-lang)

function core_format_greeting(name)
    return "Hello, " + name + "!"
end

function core_format_custom(config)
    return config.prefix + ", " + config.name + "!"
end