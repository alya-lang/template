# {{PACKAGE_NAME}} library
# Part of the Alya Language package ecosystem (https://github.com/alya-lang)

import "./types.alya"
import "./core/formatter.alya"

# Internal sanitize helper (private to facade, not exported with pub)
function internal_clean_name(name)
    if name is null
        return "World"
    end
    return name
end

# Public facade functions (exported via pub)
pub function hello(name = "World")
    let clean = internal_clean_name(name)
    return core_format_greeting(clean)
end

pub function new_config(name = "World", count = 1, style = TemplateStyle.Standard)
    let clean = internal_clean_name(name)
    return make_config(clean, count, style)
end
